# DECISIONS

2026-09-06 orchestrator B: merged U-C2 PR35-PROOF, U-C3, U-C4, U-C5 to main under the handoff's merge authority; gate `PYTHONPATH=. python3 -m unittest discover -s tests/unit | tail -1 | grep -qx OK && sh tests/smoke/run-headless.sh && test -z "$(git status --porcelain)"` rc 0; receipts under /home/tom/research-methods/receipts/FACTORY-2026-09-06/.

2026-09-06 HK-1 (hk/supervised-lane): `hk lane start|deliver|status|stop <preset.toml>` added — the supervised-lane preset (herdr-kitten #36/#37/#38 = tally.nix #670/#673/#678). Exit codes stay RULING-kitten §5's (0 delivered, 1 refused with the herdr code verbatim, 2 preset/CLI syntax, 3 no lane reachable with zero bytes sent, 4 not implemented) rather than the card's gloss, which would renumber a contract every existing verb already answers to; all five of the card's outcomes are produced and asserted under the §5 numbering (docs/dev/DEFERRED.md, HK-1 boundary). A lane is found again by its `hk_lane` metadata token, not its label, because a label is a human surface. `hk lane start` may wait, bounded and opt-in (`ready_match`/`ready_timeout_ms`), for the worker to announce itself: reporting 0 for a lane that never came up is a lie a supervisor cannot recover from, and spec F.6's no-polling rule governs interactive paths, not lifecycle ones. `tests/smoke/supervised-lane.sh` is its own oracle entry, not a member of `run-headless.sh`. The card's "no session" outcome was MEASURED rather than assumed: with no herdr server running at all, `hk lane` exits 1 with `server_not_running` verbatim — byte-for-byte the answer `hk send` already gives in that state — and with a server up but no lane of that name it exits 3 with zero bytes of stdin consumed; the smoke asserts the first case as its opening step, before it starts a server, so both halves are proven rather than documented. Two pinned surface tests were grown BY HAND for this unit (`tests/unit/test_routing.py`: the D17 verb set admits `lane`, the `conf/` tree pin admits `supervised-lane.toml`): those pins exist to catch surface drift nobody decided, so drift a card mandates is added deliberately and named here instead of by loosening the assertion.

2026-09-07 HK-1 repair (hk/supervised-lane, evaluator FAIL at 5f88268): both blocking defects repaired, each with a rerunnable probe in `tests/proofs/`. DEFECT 1 — the paste-read-back oracle accepted a non-byte-identical delivery (the fixture worker was a `while read -r line` loop, which cannot report a trailing fragment with no newline after it because `read` holds it until an EOF a live lane never sends, and the smoke compared only the last sentinel body against a newline-STRIPPED copy of the fixture: 61 bytes for a 62-byte payload, so hk appending one byte still passed). The worker is now byte-oriented — raw mode (`ICANON` off, since the tty's line discipline buffers the same fragment; `ECHO` off), reporting every chunk as it arrives both as literal hex and as its own cumulative count+sha256 — and the smoke compares ALL 62 bytes of the payload file twice: `cmp` on the bytes reconstructed from the rail, and the worker's `READBACK-SHA <count> <digest>` against `wc -c`/`sha256sum` of that file. The decision worth recording is that the fix is in the FIXTURE and the gate, not in hk: hk's delivery was already byte-exact, and what was broken was the instrument measuring it — an oracle that cannot see a byte is not a passing oracle. `tests/proofs/hk1-readback-bytes.py` mutates hk's delivery path four ways in a COPY of the tree (append, drop, strip the trailing newline, alter in place) and requires RED for each and GREEN unmutated; measured GREEN on the repair (rc 0, 5/5 held) and RED with the repair reverted (rc 1, `append-a-byte` wanted 1 got 0 with the old gate printing PASS on 61 bytes). DEFECT 2 — a partial start failure leaked an unaddressable worker and a retry duplicated it (cleanup existed only in the readiness-timeout branch, after `report_metadata`/`pane_rename`). `hk lane start` now runs every post-split step inside one cleanup boundary (`_claim`/`_abandon`), discloses the cleanup in the same typed line while herdr's code still leads it, and READS THE JOIN BACK before reporting 0 — a metadata write herdr accepted but never published is the same unaddressable state with no exception to catch, so `start` refuses to claim a lane it cannot itself resolve by name. `tests/proofs/hk1-retry-cleanup.py` measures it against a live server with one injected `report_metadata` fault: measured GREEN on the repair (rc 0; half-started pane gone from the real pane table, status 3, retry yields exactly one lane pane and baseline+1 panes total, that lane then delivers and reads back all 62 bytes) and RED with the repair reverted (rc 1; pane still open, 3 panes where 2 were wanted, residue at teardown — the evaluator's own measurement reproduced). Neither repair weakened an assertion: the timeout-closes-the-pane unit moved from `_await_ready` to `start`, which now owns residue, and still asserts the close, the exit code and the `timeout:` lead.
